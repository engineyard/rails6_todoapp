<h1>Hotwire todo list</h1>
<h5>open this page with 2 browsers to observe the live rendering upon CRUD actions.</h5>
<%= turbo_stream_from "todos" %>

<%= turbo_frame_tag "todo_form" do%>
  <%= render 'form',todo: @todo%>
<% end %>

<%= turbo_frame_tag "todos" do%>
  <%= render @todos%>
<% end %>

<%= link_to "Home page", root_path %>